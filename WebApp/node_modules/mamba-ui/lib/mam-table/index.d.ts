import * as React from 'react';
import { TableProps, ColumnProps } from 'antd/es/table';
import { PaginationConfig } from 'antd/es/pagination';
import { PopconfirmProps } from 'antd/es/popconfirm';
export interface ActionsItem<T> {
    name: string;
    onClick?: (text: any, record: T) => void;
    popconfirm?: PopconfirmOption;
}
interface PopconfirmOption extends PopconfirmProps {
    confirm?: Function;
}
export interface MamColumnProps<T> extends ColumnProps<T> {
    actions?: ActionsItem<T>[];
}
export interface MamTableProps<T> extends TableProps<T> {
    dataSource?: T[];
    columns: MamColumnProps<T>[];
    serialNo?: boolean | SerialNoOption;
}
interface SerialNoOption {
    startIndex?: number;
    columnTitle?: string;
}
interface MamTableState {
}
export default class MamTable<T> extends React.Component<MamTableProps<T>, MamTableState> {
    private columns;
    constructor(props: MamTableProps<T>);
    getPaginationConfig(): PaginationConfig | false | undefined;
    getColumnsConfig(): ColumnProps<T>[];
    getDataSource(): T[];
    render(): JSX.Element;
}
export {};
