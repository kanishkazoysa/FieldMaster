import * as React from 'react';
import SearchFormItem from './SearchFormItem';
export interface SearchFormProps {
    className?: string;
    prefixCls?: string;
    searchText?: string;
    resetText?: string;
    cols: number;
    style?: React.CSSProperties;
    onSearch?: React.MouseEventHandler<HTMLElement>;
    onReset?: React.MouseEventHandler<HTMLElement>;
}
interface SearchFormState {
    expand: boolean;
}
export default class SearchForm extends React.Component<SearchFormProps, SearchFormState> {
    static defaultProps: {
        prefixCls: string;
        cols: number;
        searchText: string;
        resetText: string;
    };
    static Item: typeof SearchFormItem;
    constructor(props: SearchFormProps);
    onExpand(): void;
    renderSubmitButton(wrap: boolean, needExpand?: boolean): React.ReactNode;
    render(): JSX.Element;
}
export {};
