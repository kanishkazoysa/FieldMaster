import { ItemValue } from '../../../Picker/components/WheelPicker/type';
import { CascadePickerItemProps, DatePickerPropsBase } from './type';
export default function useDatePicker({ mode, labelUnit, format, value, minDate, maxDate, onChange, }: Required<Pick<DatePickerPropsBase, 'value' | 'mode' | 'labelUnit' | 'format'>> & Pick<DatePickerPropsBase, 'minDate' | 'maxDate' | 'onChange'>): {
    getValueCols: () => {
        cols: CascadePickerItemProps[][];
        values: string[];
    };
    onValueChange: (value: ItemValue, index: number) => void;
};
