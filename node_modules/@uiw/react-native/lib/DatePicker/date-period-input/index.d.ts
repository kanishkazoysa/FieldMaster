import { FC } from 'react';
import { DatePickerPropsBase, ModalPickerProps } from '../components/basic-date-picker/type';
export interface DatePeriodInputProps extends Omit<DatePickerPropsBase, 'value' | 'onChange' | 'minDate' | 'maxDate'>, Omit<ModalPickerProps, 'visible' | 'displayType'> {
    /** 默认提示语 */
    placeholder?: string[];
    value?: [Date | undefined, Date | undefined];
    onChange?: (value: [Date | undefined, Date | undefined]) => void;
    /** 是否允许清除 */
    allowClear?: boolean;
    /** 是否禁用 */
    disabled?: boolean;
}
/** 适用于筛选条件下的日期区间选择 */
declare const DatePeriodInput: FC<DatePeriodInputProps>;
export default DatePeriodInput;
