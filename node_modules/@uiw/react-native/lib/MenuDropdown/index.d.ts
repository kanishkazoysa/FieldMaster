import React from 'react';
import { ButtonProps } from '../Button';
export interface MenuDropdownProps extends ButtonProps {
    title: string;
}
declare function MenuDropdown(props: MenuDropdownProps): React.JSX.Element;
declare namespace MenuDropdown {
    var Item: typeof import("./item").default;
}
export default MenuDropdown;
