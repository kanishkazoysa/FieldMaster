export declare function arrayTreeFilter<T>(data: T[], filterFn: (item: T, level: number) => boolean, options?: {
    childrenKeyName?: string;
}): T[];
/**
 * 判断元素是否在数组最后一位
 * @param length 数组长度
 * @param i 循环下标
 * @returns
 */
export declare const last: (length: number, i: number) => boolean;
/**移动数据*/
export declare const move: <T = any>(dataArray: T[], moveIndex: number, toIndex: number) => T[];
