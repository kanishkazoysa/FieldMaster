import React from 'react';
import { ViewStyle } from 'react-native';
import { size } from './index';
export declare enum containerSize {
    small = 30,
    default = 36,
    large = 44
}
export declare enum contentSize {
    small = 12,
    default = 18,
    large = 26
}
export interface DirTextProps {
    size: size;
    direction: 'left' | 'right';
    disabled: boolean;
    icon: boolean;
    onPageChange: (type: 'prev' | 'next') => void;
    borderColor?: string;
    color?: string;
}
declare const DirText: (props: DirTextProps) => React.JSX.Element;
export declare const containerStyle: ViewStyle;
export default DirText;
