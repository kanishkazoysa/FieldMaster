import { style } from './index';
export declare let MainWidth: number;
export declare let MainHeight: number;
export declare enum xLocation {
    start = "flex-start",
    center = "center",
    end = "flex-end"
}
export interface getLocationReturn {
    style: style;
    isDown: boolean;
    isStart: xLocation;
    triangle: number;
}
/**
 * 得到 cloud 位置
 * @returns getLocationReturn
 */
export declare function getLocation(width: number, height: number, pageOffsetX: number, pageOffsetY: number, _W: number, _H: number): getLocationReturn;
