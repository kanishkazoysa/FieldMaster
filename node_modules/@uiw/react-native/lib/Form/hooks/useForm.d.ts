import { KeyType, FormInstance } from '../types';
export default function useForm<FormData = any, FieldValue = FormData[keyof FormData], FieldKey extends KeyType = keyof FormData>(): FormInstance<FormData, FieldValue, FieldKey>;
