import React from 'react';
import { FormItemsProps } from '../types';
declare const Tip: ({ v }: {
    v: Partial<FormItemsProps> & {
        field: string;
    };
}) => React.JSX.Element;
export default Tip;
