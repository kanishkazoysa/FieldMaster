import React from 'react';
import Switch from '../../Switch';
const FormSwitch = ({ value, onChange, ...others }) => {
    return (<Switch checked={value} onValueChange={() => {
            onChange?.(!value);
        }} {...others}/>);
};
export default FormSwitch;
