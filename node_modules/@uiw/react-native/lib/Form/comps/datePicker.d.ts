import React from 'react';
import { ViewStyle } from 'react-native';
import { DatePickerProps } from '../../DatePicker/date-picker';
export interface RnDatePickerProps extends DatePickerProps {
    disabled?: boolean;
    placeholder?: string;
    contentStyle?: ViewStyle;
    extra?: JSX.Element;
    showClear?: boolean;
}
declare const FormDatePicker: ({ disabled, placeholder, contentStyle, extra, showClear, value, onChange, format, ...attr }: RnDatePickerProps) => React.JSX.Element;
export default FormDatePicker;
