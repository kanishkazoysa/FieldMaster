import React from 'react';
import useForm from './hooks/useForm';
function create(Com) {
    return (props) => {
        const form = useForm();
        const baseProps = {
            ...props,
            form: form,
        };
        return <Com {...baseProps}/>;
    };
}
export default create;
