/// <reference types="react" />
import { Theme } from '../theme';
interface DescProps {
    desc?: string | string[];
    theme: Theme;
}
export declare const Desc: ({ desc, theme }: DescProps) => import("react").JSX.Element;
export {};
